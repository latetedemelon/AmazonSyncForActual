{
  "name": "amazon-sync-for-actual-extension",
  "private": true,
  "version": "0.3.5",
  "description": "Browser extension that gathers Amazon order data as a CSV for Amazon Sync for Actual.",
  "license": "MIT",
  "scripts": {
    "test": "node --test"
  },
  "devDependencies": {
    "jsdom": ">=22"
  }
}
